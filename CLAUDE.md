# 梵呗内容引擎 · 项目基准

唯一开发基准文档：`docs/design-v0.8.md`（"设计文档"下称 v0.8）。本文件是从 v0.8 中提炼出的**硬性约束速查表**，每次会话自动加载。遇到任何与本文件冲突的直觉，以 v0.8 原文对应章节为准；v0.8 未覆盖处按内核原则 4 报错，不猜。

## 项目是什么

时间线驱动、内容与呈现彻底分离、主题可换肤的内容播放/创作引擎。佛教梵呗是第一个垂直场景，引擎本身对领域无知。第一性诉求是"能落地"，不是功能堆叠。

## 不可动摇的地基（违反任何一条 = 直接打回重写，不是风格问题）

1. **内容即数据**：领域逻辑（状态机、条件分支）不得写进引擎；领域知识只活在模板与数据里。
2. **零领域硬编码**：引擎代码中不得出现"本/章/句/字/遍数/會"等任何领域词；层级名由模板按深度赋予。
3. **schema 先于工具**：数据契约（v0.8 §1–§4）一旦写下即地基，框架/工具可换，schema 不可随手改。
4. **反猜语义**：缺定义 = 报错，绝不运行期兜底或替作者补内容。
5. **单一主时钟**：有音频轨时 `AudioContext.currentTime` 是唯一时间真相源；`requestAnimationFrame` 只是采样时机，禁止产生第二时钟（v0.8 §0）。
6. **f(t) 必须是纯函数**：不持有状态、不做逆操作动画；变速/循环/暂停都是这个函数之上的采样器/覆盖层行为，不得侵入内核（v0.8 §12.1, §22, §28）。
7. **视觉三层覆盖链，只覆盖不重写**：令牌 → 槽位 → 单元精修 → 观众瞬态覆盖，逐层可回落，禁止跳过令牌直接写死色值（v0.8 §5）。
8. **观众端瞬态、物理隔离**：观众意图（viewerTokens/playbackPolicy/viewerPractice）永不写回作品数据、永不进 undo，只存本地/URL（v0.8 §25–§28）。
9. **结构性错误一律前移到静态期**：schema 校验、继承环检测、fragment 冲突全部在编辑/保存期报错；运行期唯一允许的"变通"是资源降级，不做逻辑容错（v0.8 §22）。
10. **几何测量一次性缓存**：动画循环内禁止读 `getBoundingClientRect`；主渲染与缩略导航器共享同一份几何表，不得各自重复测量（v0.8 §12.3, §12.4）。

## 技术选型（已定案，不要在实现时重新讨论）

- 框架：**Svelte**（MIT）。不是待定项，待定会导致不同会话写出两套代码。
- 主渲染：DOM/CSS + `writing-mode: vertical-rl`，不用 Canvas；Canvas/WebGL 仅作局部特效层旁挂（v0.8 §13）。
- 许可证边界：只用 MIT/Apache-2.0/BSD/ISC；**明确回避 AGPL/SSPL/BUSL**（含 Remotion）。视频导出只用 Revideo（MIT）。新增依赖前先确认许可证。

## 当前所处阶段（实现顺序，见 v0.8 第十卷）

1. **Level 0（先跑通数据流）未完成前不要做任何挂件/视觉功能**：骨架 parser → 严格 JSON → f(t) 求值器 → 最小 DOM 渲染（竖排 + 逐字高亮）。
2. **Level 0 出口的三浏览器真机测试是不可跳过的强制关卡**：楞严咒 20 字 + 右侧注音 + 逐字高亮，过关才能继续铺全文（v0.8 §13.1 头号风险）。
3. **挂件契约（§7.5）先于任何挂件实现**，Level 1 前置阻塞项。
4. Level 1 出口硬指标：楞严咒全文 + 逐字高亮 + 60fps + 中端手机，过不了就回炉性能，不往下走。
5. 时间线可视化工作台已砍出第一阶段（有打点对齐后不需要），不要主动补。

## 与 AGENTS.md（Ponytail 效率规则）的关系

`AGENTS.md` 提供通用的"别过度开发"纪律，但本文件优先级更高：**v0.8 中已经明确写下的 schema 字段、契约、抽象层（五维内核、轨道/时间线/锚点、令牌/槽位/覆盖三层、挂件纯函数契约等）都算作"已被明确要求"，不受 Ponytail"未经明确要求不做抽象"规则的否决**——它们是文档作者已经拍板、写入契约的设计，不是某次窄任务里临时冒出来的花活。Ponytail 的克制只用来防止在这些已定契约之外自由发挥（多余配置项、多余状态、多余动画库）。

## 参考

- 完整设计文档：`docs/design-v0.8.md`（唯一真相源，本文件之外的细节以它为准）
- 附 B 给出了推荐的阅读/实现顺序：先读 §0–§4 建立数据模型，再读其余章节
